ko.bindingHandlers.evenRowStyle = {
    init: function (element, valueAccessor) {

        let rowIndex = $(element).index();
        let cssClassName = ko.unwrap(valueAccessor());
        
        if (rowIndex % 2 !== 0) {
            $(element).addClass(cssClassName);
        }
    }
};