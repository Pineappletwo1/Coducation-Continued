export default function Dictionary(elements) {

    function get(key) {
        for (let currentElement in elements) {
            if (key.indexOf(currentElement) > -1) {
                return elements[currentElement];
            }
        }
        return undefined;
    }

    return {
        get
    };
}