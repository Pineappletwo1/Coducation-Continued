import labeledDateInputTemplate from './labeled-date-input-template';

ko.components.register('labeled-date-input', {
    viewModel: (params) => { 
        const today = moment.utc().format();

        return {
            props: params,
            today
        };
    },
    template: labeledDateInputTemplate
});