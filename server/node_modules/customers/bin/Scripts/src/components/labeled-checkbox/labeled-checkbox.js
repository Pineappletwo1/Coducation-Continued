import labeledCheckboxTemplate from './labeled-checkbox-template';

ko.components.register('labeled-checkbox', {
    viewModel: (params) => { 
        return {
            props: params
        };
    },
    template: labeledCheckboxTemplate
});