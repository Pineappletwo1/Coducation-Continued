export default function UserRightsService(userPermissions) {

    function hasPermission(permissionId) {
        const userPermission = _.find(userPermissions, x => x.PermissionDefinition == permissionId);
        if (userPermission != undefined) return userPermission.HasPermission;
        return false; 
    }

    return { hasPermission };
}