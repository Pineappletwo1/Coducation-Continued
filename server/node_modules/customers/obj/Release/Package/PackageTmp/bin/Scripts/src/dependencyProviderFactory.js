import DependencyProvider from './dependencyProvider';

export let DependencyProviderFactory = (function () {

    let dependencyProvider;

    function get() {
        if (!dependencyProvider) {
            dependencyProvider = DependencyProvider();
        }
        return dependencyProvider;
    }

    return {
        get
    };
})();