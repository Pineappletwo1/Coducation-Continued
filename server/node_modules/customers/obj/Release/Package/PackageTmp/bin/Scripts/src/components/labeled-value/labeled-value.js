import labeledValueTemplate from './labeled-value-template';

ko.components.register('labeled-value', {
    viewModel: (params) => { 
        return { props: params };
    },
    template: labeledValueTemplate
});