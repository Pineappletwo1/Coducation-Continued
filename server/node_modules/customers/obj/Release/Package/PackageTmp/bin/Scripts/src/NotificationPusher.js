
export default function NotificationPusher() {

    function send(title, body, icon){
        Push.create(title, {
            body: body,
            icon: icon,
            onClick: function () {
                window.focus();
                this.close();
            }
        });
    }

    return {
        send
    };
}