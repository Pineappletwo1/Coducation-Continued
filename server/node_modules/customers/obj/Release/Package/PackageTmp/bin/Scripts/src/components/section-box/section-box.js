import sectionBoxTemplate from './section-box-template';

ko.components.register('section-box', {
    viewModel: (props) => { 

        const headerVisible = _.isObject(props.title);

        return { 
            props,
            headerVisible 
        }; 
    },
    template: sectionBoxTemplate
});