import RegisterComponent from '../../componentRegister';
import Template from './simple-select-template';

RegisterComponent('simple-select', {
    script: {
        create: (props) => {
            return {
                label: props.label,
                options: props.options,
                value: props.value,
                onSelected: props.onSelected
            };
        }
    },
    template: Template
});